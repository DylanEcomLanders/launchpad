/* /company/people is rendered by the layout's TabShell (see
 * layout.tsx). This stub keeps the route resolvable for deep links;
 * the layout picks the People panel based on pathname. The Person
 * detail route at /company/people/[id] renders through {children}. */
export default function PeopleRoute() {
  return null;
}
