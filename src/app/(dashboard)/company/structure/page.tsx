/* /company/structure is no longer in the top nav (it was the
 * org-chart flowchart - replaced by the By-pod grouping in People).
 * Kept reachable via direct URL for anyone who bookmarked it; layout
 * treats this path as a detail route and renders {children}. */
export { default } from "../_panels/StructurePanel";
