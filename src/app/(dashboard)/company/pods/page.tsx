/* /company/pods is rendered by the layout's TabShell (see layout.tsx).
 * This stub keeps the route resolvable for direct deep links; the
 * layout picks the Pods panel from the pathname. */
export default function PodsRoute() {
  return null;
}
