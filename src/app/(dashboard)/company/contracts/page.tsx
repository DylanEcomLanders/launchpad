/* /company/contracts is a top-level Admin tab. The layout's PanelFor
 * picks up tab="contracts" → ContractsPanel and children aren't
 * rendered for this route. Stub returns null. */
export default function ContractsRoute() {
  return null;
}
