/* Top-tab routes for /company are rendered by the layout's TabShell
 * (see layout.tsx). This page is a stub so direct visits to /company
 * still resolve - the layout decides what panel to show based on the
 * pathname. Detail routes like /company/people/[id] have their own
 * pages and render through {children}. */
export default function CompanyOverviewRoute() {
  return null;
}
