/* /company/settings is rendered by the layout's TabShell (see
 * layout.tsx). This stub keeps the route resolvable for deep links;
 * the layout picks the Settings panel based on pathname. */
export default function CompanySettingsRoute() {
  return null;
}
