import ClientSuccessPreview from "./client";

export const metadata = { title: "Client Success OS · Preview · Launchpad" };

export default function ClientSuccessPreviewPage() {
  return <ClientSuccessPreview />;
}
